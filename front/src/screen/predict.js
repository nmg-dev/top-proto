import React from 'react';

class PredictScreen extends React.Component {
    constructor(ps) {
        super(ps);
        this.state = {};
    }

    render() {
        return 'prediction';
    }
}

export default PredictScreen;